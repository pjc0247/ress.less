# ress.less

```less
selector {
  .size(30px, 40px); /* 기본값 */
  .size('tablet', 25px, 35px); /* 타블렛 화면에서 보여줄 값 */
  .size('mobile', 20px, 30px); /* 모바일 화면에서 보여줄 값 */
  
  .position(100px, 40px);
  .position('tablet', 120px, 50px);
  .position('mobile', 130px, 60px);
}
```
